{
  "name": "@contifisc/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Primeira proposta de schema.prisma canonico (draft, sem migration) do MCD-001 V1.2 / CDC-001 V1.2 / COT-001 V1.1 / ADR-001 V1.0.",
  "scripts": {
    "prisma:format": "prisma format",
    "prisma:validate": "prisma validate",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
